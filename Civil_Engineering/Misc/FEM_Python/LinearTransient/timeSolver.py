import numpy as np
from ODE_IVP_VECTOR import Runge_Kutta_vector,Kutta_step,Kutta_eqn
def ElastoDynamics(K,M,F):
    num_of_dof = len(len(K))
    d = np.matrix(np.zeros(num_of_dof,2))

def solve(K,M,F):
